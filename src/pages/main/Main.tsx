import { FC } from 'react';
import { Form } from '../../components/form/Form';
import './Main.scss';

const Main: FC = () => {
  return (
    <main className="main">
      <Form />
    </main>
  );
};

export { Main };
